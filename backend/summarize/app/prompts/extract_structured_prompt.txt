Analyze the following document and extract all structured information.
Your extraction must prioritize content relevant to this instruction: {instruction}

Return ONLY a valid JSON object with no additional text or explanation.

The JSON must have this exact structure:
{{
  "actionItems": [
    {{ "task": "string", "assignee": "string or null", "dueDate": "string or null" }}
  ],
  "decisions": [
    {{ "decision": "string", "context": "string or null" }}
  ],
  "deadlines": [
    {{ "item": "string", "date": "string", "owner": "string or null" }}
  ],
  "participants": ["string"]
}}

Field guidance:
- actionItems: every task, obligation, or required action assigned to any party
- decisions: every conclusion, determination, finding, or agreed outcome
- deadlines: every specific date tied to an obligation, milestone, deliverable, or expiry
- participants: every named person, speaker, signatory, author, or referenced individual
- Return an empty array for any category where nothing is found
- Preserve all names, dates, and numbers exactly as they appear in the source

Document:
{transcript}
